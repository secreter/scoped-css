{
  "name": "css-scoped",
  "version": "0.9.0",
  "description": "Limit the css scope by adding a uniform parent selector name to the selector, which is useful in componentized development and rendering.",
  "main": "lib/scope.js",
  "scripts": {
    "run": "node ./test/index.js",
    "test": "mocha  ./test/scope.test.js",
    "cover": "istanbul cover node_modules/mocha/bin/_mocha",
    "coveralls": "npm run cover -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "keywords": [
    "scope",
    "secoped",
    "css",
    "style",
    "selector",
    "css-module"
  ],
  "author": "so <so@redream.cn>",
  "license": "MIT",
  "bugs": "https://github.com/secreter/scoped-css/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/secreter/scoped-css.git"
  },
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.1",
    "istanbul": "^0.4.5",
    "mocha": "^5.2.0"
  }
}
